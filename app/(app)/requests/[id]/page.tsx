"use client";

import { RequestDetailPage } from "@/components/components-page/requests/request-detail-page";

export default function Page({ params }: { params: { id: string } }) {
  return (
  <RequestDetailPage />
  );
}
